# QA-02 Prep Evidence (Stage-2 Validation Pipeline)

Date: 2026-03-03  
Task state: In Progress (prep only)  
Package state: WP-03 remains Blocked pending WP-02 completion

## Scope Completed

1. Prepared benchmark/evidence template structure for Stage-2 Scenario A/B runs.
2. Validated threshold evaluation command path from repo root.
3. Confirmed expected CSV schema columns used by threshold workflow.
4. Added physical-device execution runbook/checklist for immediate QA handoff once ENG-03 lands.
5. Hardened `evaluate_thresholds.py` to fail fast on missing required CSV columns and missing Scenario A/B data.

## Artifacts Prepared

1. `docs/testing/stage-2-benchmark-runbook.md`
2. `docs/operations/evidence/wp-03/templates/README.md`
3. `docs/operations/evidence/wp-03/templates/scenario-a.csv`
4. `docs/operations/evidence/wp-03/templates/scenario-b.csv`
5. `docs/operations/evidence/wp-03/templates/stage-2-threshold-input.csv`
6. `docs/operations/evidence/wp-03/templates/notes.md`
7. `docs/operations/evidence/wp-03/runs/.gitkeep`

## Threshold Workflow Validation

Command path confirmed:

```bash
python3 scripts/benchmarks/evaluate_thresholds.py <benchmark_csv>
```

Validation checks run:

1. Header validation for stage template:
   - Header in `scripts/benchmarks/stage-scenarios-template.csv` contains `scenario`, `first_token_ms`, `decode_tps`.
2. Dry-run on populated stage template:
   - Command: `python3 scripts/benchmarks/evaluate_thresholds.py scripts/benchmarks/stage-scenarios-template.csv`
   - Result: `Overall: PASS`, exit `0`
3. Dry-run on new placeholder A/B template:
   - Command: `python3 scripts/benchmarks/evaluate_thresholds.py docs/operations/evidence/wp-03/templates/stage-2-threshold-input.csv`
   - Result: `Overall: FAIL`, exit `2` (expected until real device metrics are populated)
4. Schema failure-path check:
   - Command: `python3 scripts/benchmarks/evaluate_thresholds.py <csv missing decode_tps column>`
   - Result: `Schema Error` with missing column list, exit `1`

## Ready-For-Execution Statement

Once ENG-03 is merged and deployable, QA can immediately:

1. Create run folder under `scripts/benchmarks/runs/YYYY-MM-DD/<device-id>/`.
2. Copy prepared templates.
3. Execute Scenario A/B on physical device.
4. Run threshold evaluator and capture `threshold-report.txt`.
5. Attach run evidence to WP-03.

## Remaining Before QA-02 Full Validation

1. ENG-03 must clear physical-device acceptance in WP-02.
2. QA must execute real Scenario A/B runs (not templates) on target device.
3. Threshold report must be generated from real run CSV and attached with logcat evidence.

## Not Done Yet

1. No real physical-device Scenario A/B validation executed in this prep step.
2. QA-02 is not marked Done.
3. WP-03 remains Blocked until WP-02 completion.
