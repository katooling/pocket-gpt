# ENG-04 (WP-03) Evidence - 2026-03-03

Date: 2026-03-03  
Task state: In Progress (parallel CI-first scope)  
Package state: WP-03 remains Blocked for completion pending WP-02 physical-device unblock criteria

## Scope Delivered (CI-Safe, Device-Independent)

1. Productionized `ModelArtifactManager` artifact lifecycle for stage-2 reliability work:
   - manifest structure export for all registered model/version artifacts
   - checksum verification pass/fail/unknown result handling
   - deterministic version comparison and active model version resolution
2. Added CI-safe manifest validation hook to surface invalid artifact metadata/checksum formatting before device execution.
3. Preserved existing manager call shape used by Android container (`registerArtifact`, `setActiveModel`, `verifyChecksum`, `getActiveModel`).

## Implementation Artifacts

1. Updated artifact manager implementation:
   - `packages/inference-adapters/src/commonMain/kotlin/com/pocketagent/inference/ModelArtifactManager.kt`
2. Added comprehensive artifact lifecycle unit tests:
   - `packages/inference-adapters/src/commonTest/kotlin/com/pocketagent/inference/ModelArtifactManagerTest.kt`
3. Updated module docs for new artifact manager lifecycle APIs:
   - `packages/inference-adapters/README.md`
4. Updated stage tracker checklist item for checksum test coverage:
   - `docs/roadmap/mvp-implementation-tracker.md`

## Tests Added/Extended

1. Checksum success and mismatch status coverage.
2. Unknown model and unknown version checksum handling.
3. Deterministic version selection edge cases:
   - numeric ordering (`1.10.0` vs `1.2.0`)
   - prerelease handling (`1.2.0-rc.1` vs release)
   - pinned version behavior vs auto-latest selection
4. Manifest structure determinism (sorted model/version output).
5. Validation hook behavior for invalid checksum formatting.

## Commands Run and Outcomes

1. `./gradlew --no-daemon :packages:inference-adapters:test`
   - Outcome: PASS (`BUILD SUCCESSFUL`)
2. `bash scripts/dev/verify.sh`
   - Outcome: PASS (`BUILD SUCCESSFUL`)
   - Log artifact: `docs/operations/evidence/wp-03/2026-03-03-eng-04-verify.log`

## QA-02 Hand-off Behavior (Stage-2 Validation)

QA can now treat artifact lifecycle behavior as testable contracts in stage-2 prep and execution:

1. Artifact checksum verification yields explicit result status (`PASS`, `CHECKSUM_MISMATCH`, `UNKNOWN_MODEL`, `UNKNOWN_VERSION`).
2. Active model version selection is deterministic when multiple versions are registered.
3. Manifest structure export is stable/sorted for repeatable assertions in CI and QA checks.
4. Validation hook can be used in CI-safe checks to flag malformed artifact metadata before physical-device runs.

## WP-02 Blocker Impact and Assumptions

1. No physical Android device execution was required or performed for this ENG-04 scope.
2. WP-02 remains the gating dependency for WP-03 package completion because scenario A/B device evidence is still missing.
3. Design assumptions that remain device-dependent and unvalidated in this change:
   - final production SHA-256 values for packaged GGUF artifacts
   - on-device artifact I/O behavior under real storage/runtime constraints
4. Therefore, WP-03 is **not** marked Done by this change.

## Acceptance Criteria Status for ENG-04 Parallel Scope

1. Checksum pass/fail cases are test-covered: VERIFIED.
2. Deterministic version selection behavior is implemented and tested: VERIFIED.
3. `bash scripts/dev/verify.sh` passes: VERIFIED.
4. QA has clear artifact behavior for stage-2 validation: VERIFIED (unit-contract level, device-independent).
