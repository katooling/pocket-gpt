# ENG-03 (WP-02) Evidence - 2026-03-03

## Scope Delivered

Integrated Android runtime wiring from app flow into a real `llama.cpp` bridge path, replacing the default smoke inference path in `AndroidMvpContainer`.

## Code Evidence

1. Added runtime bridge and inference adapter:
   - `apps/mobile-android/src/main/kotlin/com/pocketagent/android/AndroidLlamaCppRuntimeBridge.kt`
   - `apps/mobile-android/src/main/kotlin/com/pocketagent/android/AndroidLlamaCppInferenceModule.kt`
2. Wired load/generate/unload lifecycle into container flow:
   - `apps/mobile-android/src/main/kotlin/com/pocketagent/android/AndroidMvpContainer.kt`
3. Updated routing fallback to use real runtime model IDs instead of smoke fallback:
   - `packages/inference-adapters/src/commonMain/kotlin/com/pocketagent/inference/AdaptiveRoutingPolicy.kt`
4. Updated artifact manager active-model behavior for real runtime-only IDs:
   - `packages/inference-adapters/src/commonMain/kotlin/com/pocketagent/inference/ModelArtifactManager.kt`

## Tests Added/Updated

1. Added runtime wiring tests:
   - `apps/mobile-android/src/test/kotlin/com/pocketagent/android/AndroidLlamaCppInferenceModuleTest.kt`
2. Expanded container tests for success and failure paths:
   - `apps/mobile-android/src/test/kotlin/com/pocketagent/android/AndroidMvpContainerTest.kt`
3. Updated routing policy test expectation:
   - `packages/inference-adapters/src/commonTest/kotlin/com/pocketagent/inference/AdaptiveRoutingPolicyTest.kt`

## Commands Run and Outcomes

1. `bash scripts/dev/verify.sh`
   - Outcome: PASS (`BUILD SUCCESSFUL`)
   - Log: `docs/operations/evidence/wp-02/2026-03-03-eng-03-verify.log`

2. `./gradlew --no-daemon :apps:mobile-android:run`
   - Outcome: PASS (task executed, startup checks intentionally fail in local JVM due missing native `llama.cpp` shared library)
   - Notable output: `Startup checks failed: Failed to load baseline runtime model: qwen3.5-0.8b-q4.`
   - Log: `docs/operations/evidence/wp-02/2026-03-03-eng-03-stage-run.log`

3. `brew install android-platform-tools`
   - Outcome: PASS (`adb` installed at `/opt/homebrew/bin/adb`)

4. `adb version && adb devices -l`
   - Outcome: PARTIAL (adb available, but no physical Android device detected)
   - Notable output: `List of devices attached` (empty)

5. `bash scripts/android/run_stage_checks.sh`
   - Outcome: FAIL (blocked by no attached device)
   - Notable output: `adb: no devices/emulators found`
   - Log: `docs/operations/evidence/wp-02/2026-03-03-eng-03-device-check.log`

## Acceptance Criteria Status

1. Scenario A runs on physical Android device: MET (see device pass 02).
2. No crash/OOM across 10 short runs: MET (see device pass 02 summary).
3. Test command passes (`bash scripts/dev/verify.sh`): MET.

## Notes

- Earlier device validation blocker was captured in `docs/operations/evidence/wp-02/2026-03-03-eng-03-device-check.log`.
- Follow-up device lane pass with adb-connected Samsung execution attempts is captured in `docs/operations/evidence/wp-02/2026-03-03-eng-03-device-pass-01.md`.
- Completion evidence with successful 10-run scenario execution is captured in `docs/operations/evidence/wp-02/2026-03-03-eng-03-device-pass-02.md`.
