# ENG-06 (WP-05) Evidence - 2026-03-03

Date: 2026-03-03  
Task state: In Progress (parallel CI-first scope)  
Package state: WP-05 remains Backlog/Not Done until full package acceptance evidence is complete

## Scope Delivered (CI-Safe, Device-Independent)

1. Replaced ad-hoc string parsing in `SafeLocalToolRuntime` with schema-driven JSON argument validation.
2. Kept allowlist enforcement strict before any payload parsing/execution.
3. Added deterministic validation error contracts for invalid payload paths.
4. Added adversarial and bypass regression tests for schema/type/unknown-field/payload-fragment failures.

## Implementation Artifacts

1. Runtime hardening implementation:
   - `packages/tool-runtime/src/commonMain/kotlin/com/pocketagent/tools/SafeLocalToolRuntime.kt`
2. JSON parser dependency for schema-driven validation:
   - `packages/tool-runtime/build.gradle.kts`
3. Regression and contract tests:
   - `packages/tool-runtime/src/commonTest/kotlin/com/pocketagent/tools/SafeLocalToolRuntimeTest.kt`
4. Documentation updates for runtime validation/error behavior:
   - `packages/tool-runtime/README.md`
   - `docs/testing/android-dx-and-test-playbook.md`
   - `docs/testing/test-strategy.md`
   - `docs/product/feature-catalog.md`
   - `docs/roadmap/mvp-implementation-tracker.md`

## Validation Contract Implemented

All validation rejections now return deterministic contract format:

`TOOL_VALIDATION_ERROR:<ERROR_CODE>:<DETAIL>`

Error-code paths covered by tests include:

1. `NOT_ALLOWLISTED`
2. `INVALID_JSON`
3. `MISSING_REQUIRED_FIELD`
4. `UNKNOWN_FIELD`
5. `INVALID_FIELD_TYPE`
6. `INVALID_FIELD_VALUE`

## Tests Added/Extended

1. Positive execution test for valid calculator payload.
2. Malformed JSON rejection with deterministic error assertion.
3. Schema-invalid type rejection (`expression` non-string).
4. Missing required field rejection.
5. Unknown field rejection (bypass attempt).
6. Non-allowlisted tool rejection.
7. Adversarial fragment rejection (`exec`) including escaped-unicode bypass attempt.
8. Nested-object payload rejection for string fields.
9. `validateToolCall` pass path for schema-valid payload.
10. Date-time tool extra-field rejection.

## Commands Run and Outcomes

1. `./gradlew --no-daemon :packages:tool-runtime:test`
   - Outcome: PASS (`BUILD SUCCESSFUL`)
   - Log artifact: `docs/operations/evidence/wp-05/2026-03-03-eng-06-tool-runtime-test.log`
2. `bash scripts/dev/verify.sh`
   - Outcome: PASS (`BUILD SUCCESSFUL`)
   - Log artifact: `docs/operations/evidence/wp-05/2026-03-03-eng-06-verify.log`

## Acceptance Criteria Status (ENG-06 Parallel Scope)

1. Malformed JSON and schema-invalid payloads are rejected deterministically: VERIFIED.
2. Non-allowlisted tool calls cannot execute: VERIFIED.
3. Adversarial payload regression tests are green: VERIFIED.
4. `bash scripts/dev/verify.sh` passes: VERIFIED.
5. Documentation reflects validation/error behavior: VERIFIED.

## Dependency/Status Notes

1. No physical Android device execution is required for this task scope.
2. WP-05 is not marked Done by this update.
3. Full WP-05 completion still depends on broader package acceptance evidence and stage/package gating.
