interface:
  display_name: "Maestro Android CLI"
  short_description: "Run and debug Maestro Android flows"
  default_prompt: "Use $skill-name to run doctor, lane, scoped, report, trace, and cloud workflows for a PocketGPT Android project."

policy:
  allow_implicit_invocation: true
