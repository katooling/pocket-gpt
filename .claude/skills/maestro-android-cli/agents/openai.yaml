interface:
  display_name: "Testing Android with Maestro"
  short_description: "Orchestrates PocketGPT Android testing via maestro-android CLI"
  default_prompt: "Use $skill-name to run lanes, scoped repros, audits, failure triage, and cloud workflows for PocketGPT Android testing."

policy:
  allow_implicit_invocation: true
